{
  "name": "password-validator",
  "version": "1.0.0",
  "description": "Sistema de validação de senhas seguras - TDD",
  "main": "src/passwordValidator.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
